import { Category } from '../types/Category';

export const categories: Category = {
    rent: { title: 'Saida', color: 'brown', expense: true },
    salary: { title: 'Entrada', color: 'green', expense: false }
}